# Security MCP

- Follow `docs/development/security-integrations.md` when adding or changing an integration: it holds the contract,
  handler, output, caching, and scanner conventions every module in `integrations/` implements.
