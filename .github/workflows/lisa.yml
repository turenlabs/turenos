name: Lisa

# pull_request_target so fork PRs get secrets. Safe only because this job never
# checks out PR code — do not add a checkout step.
on:
  pull_request_target:
    paths-ignore: ["packages/*-wasm/**"]

concurrency:
  group: lisa-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lisa:
    name: lisa
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: turenlabs/lisa@3f5c01002f6e209c2fc2ac2f45b7773ff329b47e # v1.1.0
        with:
          api-key: ${{ secrets.TYPESAFE_API_KEY }}
